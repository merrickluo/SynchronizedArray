source 'https://github.com/CocoaPods/Specs.git'
use_frameworks!

target 'SynchronizedArray_Example', :exclusive => true do
  pod 'SynchronizedArray', :path => '../'
end

target 'SynchronizedArray_Tests', :exclusive => true do
  pod 'SynchronizedArray', :path => '../'

  pod 'Quick', '~> 0.8'
  pod 'Nimble', '~> 3.0'
end
